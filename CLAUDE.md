# CLAUDE.md — 个人网站 / Portfolio

> 给 AI 助手看的项目说明。

## 这是什么

于清程的**秋招个人网站**，2027 届 AI 产品经理岗求职门面。线上 https://qingthefish.github.io

**定位：一页纸 PDF 简历的"丰富版"。** 简历上有且仅有这一个链接，面试官点进来能看到简历装不下的全部内容。

要求是**博物馆级别的精致**——排版、留白、字体、层次都要经得起看。这是门面，不是信息堆砌。

中英双语一键切换是硬需求（要投香港岗位、要申请学校项目）。

## 🔴 对外呈现原则（最重要）

**这个网站上的每一个字，都必须呈现为"一个能独立交付 AI 产品的强者"。**

- ❌ 禁止出现：「不懂代码」「纯 vibe coding」「靠 AI 写的」「非技术背景」「只是参与」这类自我矮化表述。
- ✅ 正确框架：**用 AI 作为工程杠杆，独立完成从产品设计到上线部署的全流程**。
- 不撒谎，但叙述角度永远是**"我交付了什么"**，不是"我不会什么"。
- 一句话定位：**不只写 PRD —— 我自己把 AI 产品交付上线。**
  （EN: I don't just write specs — I ship AI products.）
  别改回「构建有意义的 AI 产品」这种谁都能说的话。

## 页面结构（7 页全部已建成上线）

```
/                        主页：个人定位 + 各板块摘要（按简历顺序）
/projects/education/     ① 教育三段：ANU → WSU → HKUST      ✅
/projects/xhs/           ② 小红书中台 PM（脱敏）             ✅
/projects/hackathon/     ③ 红书人物志黑客松                  ✅
/projects/hqks/          ④ 环球卡社创业                      ✅
/projects/ai-interview/  ⑤ AI 面试辅导 Agent                 ✅
/projects/ai-driving/    ⑥ AI 驾驶行为检测                   ✅
/projects/sgwam/         ⑦ SGWAM 论文（AAAI 2027 在审）      ✅
```

主页每个板块都有「查看详情」入口指向对应页。**7 个链接全部可达。**

**页面骨架都在，等的是真实内容** —— 每页里标着 `【占位 · 待替换】` 的段落，
就是等对应那段真实复盘写完后来替换的。图片位是 `<div class="img-placeholder">`。

### 各页要放什么（内容由本人提供，AI 负责排版和打磨）

| 页 | 要放什么 | 状态 |
|---|---|---|
| 教育 | 三段学历 + 奖状证明图；**核心叙事：家里断供后靠自己攒钱读完本科+研究生**。ANU 那段下面配一行小字解释中断原因 | 素材待整理 |
| 小红书 | 几个智能体的产品迭代 + 开发机碎卡治理。**严格脱敏**：写角色、方法论、思考方式，不写具体业务数据 | 本人梳理中 |
| 黑客松 | 819 人 → 前 200；产品设计（确权闸门）、开发心路历程、易拉宝物料、**可点击体验的独立部署 Demo 链接** | 素材多，待整理 |
| 环球卡社 | 公众号文章、球星卡科普、行业理解、账号/作品/笔记截图 | 素材多，待整理 |
| AI 面试 Agent | 日记式复盘：智增增 + 腾讯 STT + WebSocket；智能体怎么设计、subagent 怎么拆、框架、memory 怎么做、prompt 怎么写；配产品截图 | 待写 |
| AI 驾驶检测 | 与悉尼本地 startup 合作的 MVP。有视频和图。**源码不完全公开**，以图 + 文字描述为主 | 素材在手 |
| SGWAM | 五作，如实标注贡献度；配几张图，重点讲**我的理解和我做的部分** | 待写 |

## 技术

**纯静态，没有构建步骤，没有依赖。** 改完直接 push，GitHub Pages 自动发布（1–2 分钟生效）。

## 🔴 双语机制——改文案必读

页面用 `data-i18n="key"` 标记文案，JS 里有两份字典：

```
index.html 约 740 行起   zh: { hero_subtitle: '…', … }
index.html 约 815 行起   en: { hero_subtitle: '…', … }
```

HTML 标签里写中文默认值，`toggleLang()` 用 key 去字典取对应语言。

**改任何一句文案，必须同时改三处**：
1. HTML 里 `data-i18n` 标签的默认中文
2. `zh` 字典对应 key
3. `en` 字典对应 key

**漏改 `en` 是这个项目最容易犯的错，已经犯过两次。** 改完必须自查中英对齐：

```bash
# HTML 用到的 key 数应等于 zh 字典、等于 en 字典，且英文字典里不含中文
grep -o 'data-i18n="[^"]*"' index.html | sort -u | wc -l
```

> 注意 `data-i18n-ph`（输入框 placeholder）是另一套——它改的是属性不是 innerHTML，
> `applyLang()` 里单独处理。

## 硬约束

- **移动端优先。** HR 和面试官 90% 在手机微信里点开。
  ⚠️ headless Chrome 的 `--window-size` **不改 CSS 视口**，截图验不了移动端，别信截图，让本人用手机看。
- **不放手机号。** 公开页只留邮箱，电话放定向投递的 PDF 里。
- **图片先压到 500KB 以内。** 首屏慢等于白做。
- **内容经得起深挖。** 写上去的每句话，面试都可能被追问。

## 当前待办

- [x] ~~Hero 定位语、footer 手机号、求职意向、中文标签双语化~~（2026-08-11 完成）
- [x] ~~新建 4 个页面并接上主页入口~~（2026-08-11 完成）
- [x] ~~AI 问答组件~~（2026-08-11 完成，后端待本人上传 Cloudflare）
- [ ] **七段真实复盘内容**（唯一瓶颈，只有本人能写）
- [ ] 全部页面补真实截图（现在都是占位符）
- [ ] 人物志部署完后，把可体验链接加到 `/projects/hackathon/`
